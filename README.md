# multiplication-app-node
Aplicación de consola de multiplicación desarrollada con Node.js y Typescript

## Notas

- Ejecutar `npm install` para instalar las dependencias.
- Ejecutar `npm run dev` para iniciar la aplicación.